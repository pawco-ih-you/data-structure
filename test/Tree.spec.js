const tree = require('../src/Tree')

describe('Tree', () => {
  describe('Node class', () => {
    describe('constructor', () => {
      it('stores both data and links to other nodes(left and right)', () => {

      })
    })
    describe('show', () => {
      it('displays the data stored in a node')
    })
  })
  describe('BST', () => {
    describe('constructor', () => {
      it('it sets the root node to null, creating an empty node', () => {

      })
    })
    describe('insert', () => {

    })
  })
})

